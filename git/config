[user]
  name = Hans Ellegård
[core]
  autocrlf = input
  editor = editor.fish
  pager = less -F -X
[color]
  pager = true
  ui = auto
[merge]
  guitool = meld
  tool = nvimdiff
[diff]
  guitool = meld
  tool = nvimdiff
[difftool]
  guiDefault = true
  prompt = false
[mergetool]
  guiDefault = true
  keepBackup = false
  prompt = true
[alias]
  cleantree = "!git clean -dfx; git submodule foreach --recursive git clean -dfx"
  list = log --date='format:%F %R' --pretty='format:%Cred%h %Cgreen%ad %C(bold yellow)%D %Creset%s'
[protocol.file]
  allow = always
[pull]
  ff = only
